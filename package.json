{
  "name": "monorepo-batteries-included",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "pnpm --filter vite-app run dev",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "8.0.2",
    "lint-staged": "13.1.0",
    "prettier": "2.8.0",
    "turbo": "1.6.3"
  },
  "lint-staged": {
    "**/*": "prettier --write --ignore-unknown"
  }
}
